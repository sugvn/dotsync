PushChanges:
        backup directories in the original paths and delete them only on successful pushing 
        if pushing failed,restore their original state using tmp backup directory

PullChanges/PushChanges:
         Hande special file permissions like write protected on both src and destination
